BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[admin] ALTER COLUMN [password] VARCHAR(255) NOT NULL;

-- AlterTable
ALTER TABLE [dbo].[superAdmin] ALTER COLUMN [password] VARCHAR(255) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
